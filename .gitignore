# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/

# IDE
.vscode/
.claude/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Logs
*.log

# Historical PDF archive used only for one-off backfills (data/backfill.py).
# It's ~143M and not part of Repo A's deliverable, so keep it out of git.
# data/README.md is still tracked.
pdf_files/
data/pdf_files/

# sbi_rates.db is intentionally committed — do NOT add *.db or *.sqlite here
